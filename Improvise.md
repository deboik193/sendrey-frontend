To prevent runners from marking an item as "delivered" when they haven't reached the location, Delivery Verification Code can be implemented.

The Logic: The user gets a 4-digit OTP. The runner cannot complete the trip on their app unless they type in the code provided.